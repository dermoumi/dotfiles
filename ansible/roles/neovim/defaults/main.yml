---
mise_bin: "{{ ansible_env.HOME }}/.local/bin/mise"

# Path the nvim config expects for its python3 provider
# (nvim/lua/core/options.lua).
nvim_venv: "{{ ansible_env.HOME }}/.local/share/venvs/nvim-venv"

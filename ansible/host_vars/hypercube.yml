# hypercube runs the tailnet ssh role on top of whatever variant it's given.
# Run ssh-only today with `./setup.sh` (no variant); later `./setup.sh workstation`.
host_extra_roles:
  - ssh
  - ip-forward

ssh_port: 11967
